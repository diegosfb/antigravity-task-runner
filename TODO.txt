Instead of message

To finish this PR flow:
1. Stay on feature/feat4.
2. Run 'Pull Remote and merge' first so you can verify the merge with main works before opening the PR.
3. Resolve any merge issues and rerun Create Pull Request.

Use message 

PR creation aborted! To finish this PR flow:
1. Stay on feature/feat4.
2. Run 'Pull Remote and merge' and Resolve any merge issues (verify the merge with main works before opening the PR again).
3. Try 'Create Pull Request' again

What is this? DEPRECATED

-----------------

antigravity folder	Lets you browse the home Antigravity folder at ~/.gemini/antigravity.

Deberia ser /Users/diego.brihuega/.antigravity ???
Cual es la politica entre gemini and antigravity folders?

Skills what skills from where is it showing?
Workflows not showing up.

-----------------

Pull Remote and merge does not use an agent.

That sidebar item directly launches the shell script src/pull_remote_and_merge.sh (line 1) from src/extension.ts (line 3634). The flow is script-driven:

it may ask for a commit message if you have uncommitted changes - It should call the Commit Item instead of asking for a message. 

it checks out main and pulls origin/main

it switches back to your feature branch

it merges main into that branch

it runs the project test command if configured

it pushes the branch

Check and improve. ASk llm for advice

----------------

Assign Jira Item to Agent	Lets you choose an eligible Jira item and an agent, updates the Jira summary and labels, assigns the issue to you, moves it to In Progress, and launches the selected agent for that issue.
Does it work like that? 

------------------

Also add a settings section to the file explaining all the settings to the Knowhow/help.md

------------------  

Run liteLLM OpenAI add it as a setting. [DEPRECATED FOR NOW]

------------------  

Create feature branch 
Detected uncommitted changes on main. They must be committed before creating a new branch.
Enter a commit message for the changes that should be included before creating the new branch. 
Should use the commit item instead of asking for a message  

Check if it works like this?

------------------  

1- USE /Applications/kdiff3.app/Contents/MacOS/kdiff3 file1.txt file2.txt to compare files.
Meld Diff Error: Error running diff command! ErrorCode: 127 StdErr: /bin/sh: meld: command not found

------------------  

TEST the Auxiliary Agents and Skills section

------------------  

Obsidian Vault Visualization

Add item Obsidian Vault Visualization before Autocommit Start; and when clicked run scripts/open-obsidian-vault.sh

------------------  
SOP Manual - Update

------------------  

Add item ADLC Framework Manual item after SOP Manual and when clicked open .agents/documentation/Agentic Workflows/workflow-design/agent-orchestrated-sdlc.mmd in preview and .agents/documentation/Agentic Workflows/workflow-design/ADLC-design.md