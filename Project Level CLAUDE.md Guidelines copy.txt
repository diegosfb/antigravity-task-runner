/init 

After the project's CLAUDE.md is created; update it so that:
1) Indicates that as you do work you need to commit things to Git and push them to GitHub regularly with clean commit messages so we never lose the status or work that we have and we can go back to a previous state if needed version if required. 
2) **OVERRIDE:** This project explicitly authorizes Claude to commit after each meaningful unit of work without waiting for an explicit user request. This overrides any built-in default that requires explicit permission before committing.
3) Auto update project's CLAUDE.md file regularly to capture the progress of the project.  
4) Ask me questions to clarify the product requirements, technical requirements, engineering principles, and hard constraints before making any changes to the codebase.