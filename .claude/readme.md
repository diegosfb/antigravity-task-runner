```sh
settings.json (Project-Level)Sharing: Committed to Git and shared with everyone working on the repository.Purpose: Stores team-wide rules, shared hooks, default permission policies, and approved Model Context Protocol (MCP) servers for the project.Best for: Standardizing behavior and tool access for all developers contributing to the codebase.settings.local.json (Local Override)Sharing: Automatically ignored by Git (.gitignored) so it never gets committed.Purpose: Stores your personal permission overrides, custom local configurations, or experimental settings for that specific project.Precedence: Takes higher priority than the shared settings.json, meaning any rule or permission defined here will override the team settings on your machine.Best for: Bypassing a shared restriction locally or adding private environment-specific tweaks without affecting your teammates.
```