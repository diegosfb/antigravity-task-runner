interface:
  display_name: "To Tickets"
  short_description: "Create tracer-bullet delivery tickets"
  default_prompt: "Use $to-tickets to turn this approved specification into dependency-aware delivery tickets."

policy:
  allow_implicit_invocation: false
